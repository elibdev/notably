database:
  provider: "dynamodb"
  table_name: "timedb"
  region: "us-east-1"
  endpoint_url: "http://dynamodb-local:8000"

server:
  port: 8080
  host: "0.0.0.0"

auth:
  jwt_secret: "local-development-secret"
  token_expiry: "24h"
  require_auth: false

logging:
  level: "debug"
  format: "text"
